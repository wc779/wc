# 细胞自噬原理与研究进展（PPT文件说明）

仓库中包含：

1. `细胞自噬原理与研究进展_PPT.html`：可直接用浏览器打开并按方向键播放的完整幻灯片版本。
2. `generate_autophagy_ppt.py`：使用 `python-pptx` 生成可编辑 `.pptx` 文件的脚本。

## 生成 PPTX

```bash
pip install python-pptx
python generate_autophagy_ppt.py
```

脚本将在当前目录生成：`细胞自噬原理与研究进展.pptx`。

## 演示 HTML 版本

直接打开 `细胞自噬原理与研究进展_PPT.html`，使用以下按键操作：

- `→`、`PageDown` 或空格：下一页
- `←`、`PageUp`：上一页
- `Home`：首页
- `End`：末页

内容依据原有 15 页提纲扩展，包含研究背景、类型、mTOR–ULK1–Beclin1–LC3机制、自噬流检测、选择性自噬、肿瘤、神经退行性疾病、感染、衰老、转化研究和参考文献。
